<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.javaup.mapper.VoucherMapper">
<!-- select的id和方法名要对应上  resultType对应返回返回的实体类  参数类型是我们的long类型-->
    <select id="queryVoucherOfShop" resultType="org.javaup.entity.Voucher" parameterType="java.lang.Long">
        SELECT
            v.`id`, v.`shop_id`, v.`title`, v.`sub_title`, v.`rules`, v.`pay_value`,
            v.`actual_value`, v.`type`, sv.`stock` , sv.begin_time , sv.end_time
        FROM tb_voucher v
        LEFT JOIN  tb_seckill_voucher sv ON v.id = sv.voucher_id        <!-- 左连接查询，因为优惠券的信息肯定存在，但是秒杀优惠券的信息不一定存在-->
        WHERE v.shop_id = #{shopId} AND v.status = 1     <!--根据商铺id且状态为1查询优惠券 -->
    </select>
</mapper>
